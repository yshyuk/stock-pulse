# Changelog

모든 주요 변경 사항은 이 파일에 기록됩니다.
형식: [Semantic Versioning](https://semver.org/lang/ko/)

---

## [0.3.4] - 2026-09-21

### 버그 수정
- **2차 분석이 안전 분류기에 거절되던 문제**: 자동 실행이
  `safeguards flagged this message` / `[reasoning_extraction]` 로 실패했다.
  프롬프트의 "되묻지 마세요 / 본문만 출력하세요 / 근거를 밝히세요" 조합이 탈옥 시도
  템플릿과 형태가 비슷했다. 금지문을 맥락 설명으로 바꿔(자동 배치가 텔레그램으로 보내는
  글이며 한 번에 완결된다는 사실을 먼저 말한다) 요구 동작은 유지하되 표현을 자연스럽게 했다.
  거절은 모델별 분류기이므로 `--model` / `--fallback-model` 을 명시한다
  (기본 sonnet → opus, `ANALYSIS_MODEL` / `ANALYSIS_FALLBACK_MODEL` 로 변경 가능).

### 추가
- **배포 스크립트 `deploy/install.sh`**: 배포 대상이 jar 하나가 아니라
  jar + `analyze-report.sh` + `analysis-prompt.md` 셋인데 전부 수동 복사였고,
  실제로 스크립트만 구버전으로 남아 하루치 분석이 빠진 적이 있다. 빌드 → 복사 → 검증을
  한 번에 하고, 복사 후 `cmp` 로 레포와 일치하는지 확인한다. 배포 전 브랜치·커밋·버전을
  출력하고 커밋되지 않은 변경이 있으면 경고해 브랜치 착각 배포를 막는다.

---

## [0.3.3] - 2026-09-21

### 버그 수정
- **주말에 유령 스냅샷이 쌓이던 문제**: `KrxAllStocksSource`가 데이터를 찾을 때까지 거슬러
  올라가므로 토요일 실행에서도 금요일 데이터가 수집되고, `EmptyRunPolicy`는 "데이터 0건"일
  때만 주말을 건너뛰어 스킵이 발동하지 않았다. 09-18(금) 종가가 09-19·09-20·09-21 세 날짜에
  저장됐다. 파생지표가 날짜가 아닌 행 순서로 N일 전을 세므로 20일 지표가 계속 밀렸다.
  주말이면 수집 결과와 무관하게 SKIP 하도록 바꿨다.
- **2차 분석이 실패 원인을 남기지 않던 문제**: `claude`는 인증 실패를 stderr가 아니라
  stdout으로 내는데 실패 시 stderr만 기록해, `OAuth session expired`라는 단서가 로그에도
  알림에도 남지 않았다. stdout을 우선 기록하고 인증·타임아웃은 조치 방법까지 안내한다.
- **워치독이 stdout을 점유하던 문제**: 워치독 서브셸이 스크립트의 stdout을 상속해 본체가
  끝나도 파이프가 닫히지 않았다. 운영값(600초)이면 매일 10분씩 작업이 실행 중으로 남는다.
- **배치가 늦게 끝나면 그날 분석이 빠지던 문제**: 맥이 예약 시각에 자고 있으면 배치가 늦게
  끝나는데(실측 06:00~06:31) 분석이 06:10에 한 번 보고 포기했다. 평일에는 최대 30분까지
  리포트를 기다리고 주말에는 즉시 종료한다.

### 변경
- **2차 분석 인증을 `ANTHROPIC_AUTH_TOKEN`으로 전환**: `claude`의 OAuth 세션은 만료되며
  만료되면 자동화가 멈춘다. `claude setup-token`이 발급하는 장수명 토큰을 사용한다.
  `ANTHROPIC_API_KEY`는 API 종량제 경로라 구독을 쓰지 않으므로 사용하지 않는다.
- **분석 작업을 레포가 아닌 `~/apps`에서 실행**: `~/Documents`는 macOS가 보호하는
  폴더(TCC)라 launchd 에이전트가 읽지 못해 `exit 126`으로 죽었다.

---

## [0.3.2] - 2026-09-15

### 새 기능
- **Claude Code CLI 기반 2차 분석** (`deploy/analyze-report.sh`): 배치와 분리된 launchd
  작업이 06:10에 리포트를 읽어 `claude -p`로 해석하고 텔레그램으로 보낸다.
  기존 `AnthropicReportAnalyzer`는 `api.anthropic.com`을 호출해 토큰당 과금되고 Claude Max
  구독과 별개로 청구되지만, CLI 경로는 구독 범위에서 동작한다.
  배치에 넣지 않은 이유는 과금 경로 차이 외에도, 프롬프트를 jar 재빌드 없이 고칠 수 있고
  분석 실패가 리포트 생성에 영향을 주지 않기 때문이다.
  프롬프트(`deploy/analysis-prompt.md`)는 비대화형 실행에 맞춰 되묻기를 금지하고
  (지시가 없으면 실제로 질문을 반환한다), 리포트 구조·스크리닝 룰·데이터 제약을 설명한다.

---

## [0.3.1] - 2026-09-13

### 버그 수정
- **소스 간 종목 중복으로 배치가 실패하던 문제**: KRX(전종목)와 네이버(워치리스트)를 동시에
  활성화하면 워치리스트 종목이 두 번 수집되는데, `daily_stock_snapshot`이
  `(symbol, trade_date)` 유니크라 upsert가 제약을 위반해 런 전체가 죽었다.
  `MetricProcessor`에서 심볼당 하나만 남기고, 채택 소스는
  `stockpulse.collector.source-priority`로 결정한다(기본 `krx-all > naver-finance > dummy`).
  도착 순서는 동순위일 때만 쓰므로 재실행 결과가 동일하며, 어느 쪽을 버렸는지 WARN으로 남긴다.

---

## [0.3.0] - 2026-09-11

### 중대 결함 수정 (데이터 신뢰성)
- **하드코딩 샘플 데이터가 프로덕션 리포트로 나가던 문제**: `DummyDataSource`가 기본 활성인데다
  배포 plist에 실데이터 소스 환경변수가 없어, 배치가 정상 종료(exit 0)하면서 가짜 시세
  (삼성전자 78,900 — 실제 270,000)로 리포트를 생성하고 있었음. 더미 소스를 명시적 opt-in으로
  전환하고 리포트에 데이터 출처를 표기.
- **`NaverDataSource` 파싱 전멸**: 네이버가 응답 필드를 전면 변경(`cd/nm/nv/pcv/aq/cr` →
  `itemCode/stockName/closePriceRaw/...`)했으나 테스트 픽스처가 구 스키마 기준이라
  통과하고 있었음. 실응답 캡처 기반 픽스처로 교체.
- **`KrxSupplyDemandSource` 제거**: data.krx.co.kr 포털이 프로그램 접근을 차단(400 LOGOUT)해
  동작한 적이 없음.
- **CI/배포 워크플로가 폐지된 `main` 브랜치 참조**: `release`에 머지해도 배포가 트리거되지 않고,
  `develop` 대상 PR이 검증 없이 머지될 수 있는 상태였음.
- **WebClient 버퍼 부족**: 기본 256KB로 코스피 응답(293KB)조차 받지 못해 정상 200 응답이
  `DataBufferLimitException`으로 실패. 8MB로 상향.

### 새 기능
- **KRX 전종목 수집** (`KrxAllStocksSource`): 공식 Open API로 코스피+코스닥 약 2,765종목을
  2회 호출로 수집. 장 마감 후 게시되므로 직전 거래일부터 거슬러 올라가 공휴일을
  시장 캘린더 없이 처리.
- **이상징후 스크리닝** (`ScreeningStage`): 전종목을 그대로 2차 분석에 넘기면 1회 약 42만 토큰
  (월 약 $46)에 출력도 잘리므로, 유동성 게이트 → 룰 매칭 → 랭킹 → 상한으로 좁혀 전달.
  매수/매도 판단이 아니며 정렬 기준에 상승·하락 편향이 없음. 플랜 스테이지는 전체 스냅샷 유지.
- **빈 실행 가드** (`EmptyRunPolicy`): 평일에 실가격 0건이면 배치 실패(exit 1) + FAILURE 알림.
  주말은 정상 종료. 종전에는 수집 0건도 성공으로 처리됐음.
- **인증키 만료 사전 경보** (`CredentialExpiryChecker`): 만료 30/14/7/3/2/1일 전 알림.
  재발급에 승인 대기가 필요해 리드타임이 있어야 하며, 매일 발송은 무시를 학습시키므로
  임계일에만 발송.

### 성능
- **스냅샷 upsert N+1 제거**: 종목별 존재 확인 2,765회를 1회 조회로 대체 (6.4초 → 0.5초).

### 내부 개선
- `DataSource.collect(LocalDate)` — runDate 전달로 과거 날짜 백필 가능.
- `DerivedMetricCalculator`가 소스 제공 전일종가를 우선 사용 — 콜드 스타트에서도 1일 지표가
  계산되고, 스냅샷 공백 시 잘못된 1일 변화율도 방지.
- KRX 401 응답 시 '키 만료'와 'API별 이용신청 누락' 두 원인을 모두 안내.

---

## [0.2.0] - 2026-07-27

### 저장소 무결성 수정 (중요)
- **`plan/` 패키지 누락 복구**: `.gitignore`의 앵커 없는 `plan/` 규칙이 런타임 산출물뿐 아니라
  `src/main/java/com/stockpulse/plan` 소스 패키지까지 무시해, 플랜 패키지 전체(main+test)가
  v0.1.0까지 한 번도 커밋되지 않았음(클린 clone 시 컴파일 불가). 규칙을 `/plan/`·`/reports/`로
  루트 앵커해 소스 패키지를 보호하고 패키지 전체를 git에 편입. (#7)

### 새 기능
- **트레이딩 플랜 schema v2 (stock-api signal 연동)** (#7): `mode = plan-only | signal`,
  `PlanCandidate.priority`(결정적 랭킹), v1·v2 동시 지원 직렬화(구 플랜 재읽기 호환).
- **PlanDispatcher** (#7): `signal` 모드 플랜을 외부 stock-api SignalIngest로 전송(기본 off,
  전송 실패는 fail-safe로 배치 비중단). `plan-only` 플랜은 전송 거부.
- **M4 매매 결과 환류** (#7): 전일 실현손익 + 보유 포지션을 다음날 아침 리포트의
  "전일 매매 요약" 섹션으로 표기 — 플랜 → 실행 → 리포트 폐루프 완성.

---

## [0.1.0] - 2026-07-23

StockPulse 첫 릴리즈 — **야간 배치(Part 1)** 와 **장중 실행 엔진(Part 2 M1+M2, dry-run/paper)**.

> ⚠️ 이 릴리즈는 `plan/` 소스 패키지가 gitignore로 누락되어 클린 clone 시 컴파일되지 않습니다.
> v0.2.0에서 복구되었습니다.

### 새 기능
- **시계열 축적 · 파생 지표** (#2): 일일 스냅샷 + 파생 지표 7종, 데이터 부족 시 NULL.
- **트레이딩 플랜 산출** (#2): 규칙 엔진 → `plan/YYYY-MM-DD.json`(+ DB), 리포트 요약 병기.
- **시장 지표 수집** (#2): 네이버 지수 · ECOS 환율 · KRX 수급, 플랜 `marketContext` 연동.
- **날짜 지정 재실행** (#2): `--stockpulse.run-date`로 과거 날짜 재생성(멱등).
- **장중 실행 엔진 M1** (#4): BrokerClient 추상화 + FakeBrokerClient, 멱등 주문 + 5겹 리스크 가드.
- **재시작 리컨실리에이션 M2** (#4): 브로커 보유와 로컬 포지션 대조(브로커가 진실의 원천).

### 버그 수정 · 보안
- **보안 하드닝** (#5): 킬스위치 파일 영속화, 실전 게이팅, API loopback 제한,
  자격증명 로그 유출 차단, SELL 경로 검증.
